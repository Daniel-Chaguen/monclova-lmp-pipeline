# Solo para los notebooks de notebooks/model_selection/ y 2_EDA.ipynb.
# No se instala en el entorno de produccion.
-r requirements.txt
tensorflow>=2.15      # LSTM (comparacion, descartado por desempeno)
scikit-learn>=1.3     # Isolation Forest, LOF, TimeSeriesSplit, RandomizedSearchCV
scipy>=1.11
jupyter>=1.0
seaborn>=0.13
