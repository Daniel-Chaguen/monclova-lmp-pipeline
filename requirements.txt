# Pipeline de produccion.
# LSTM, ARIMAX, Isolation Forest y LOF viven solo en los notebooks de
# model_selection, no en el camino recurrente.
pandas>=2.0
numpy>=1.24
requests>=2.31
PyYAML>=6.0
xgboost>=2.0
scikit-learn>=1.3     # requerido por la API sklearn de XGBoost (XGBRegressor)
statsmodels>=0.14     # MSTL, usado por el detector de anomalias
matplotlib>=3.7
pytest>=7.4           # tests/test_invariants.py